# CommitLog MVP

Автоматизированный виджет чейнджлога, который синхронизирует коммиты из GitHub и генерирует красивые ленты обновлений для встраивания на сайты клиентов.

## 🚀 Возможности

- **Автоматическая синхронизация** - Парсинг Conventional Commits из GitHub
- **Красивый UI** - Современный виджет для вашего сайта
- **Редактирование** - Модерация и редактирование записей перед публикацией
- **Кастомизация** - Настройка цвета и позиции виджета
- **Монетизация** - Бесплатный и Pro тарифы

## 📋 Требования

- Node.js 18+
- PostgreSQL база данных (рекомендуется Supabase)
- GitHub OAuth приложение

## 🛠️ Установка

### 1. Клонирование и установка зависимостей

```bash
# Установка зависимостей основного приложения
npm install

# Установка зависимостей виджета
cd widget
npm install
cd ..
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните переменные:

```env
# Database (Supabase)
DATABASE_URL="postgresql://user:password@host:5432/database"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key"

# GitHub OAuth
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```

### 3. Создание GitHub OAuth приложения

1. Перейдите на https://github.com/settings/developers
2. Нажмите "New OAuth App"
3. Заполните:
   - **Application name**: CommitLog
   - **Homepage URL**: `http://localhost:3000`
   - **Authorization callback URL**: `http://localhost:3000/api/auth/callback/github`
4. Скопируйте Client ID и Client Secret в `.env`

### 4. Настройка базы данных

```bash
# Генерация Prisma клиента
npx prisma generate

# Создание таблиц в БД
npx prisma db push

# (Опционально) Открыть Prisma Studio
npx prisma studio
```

### 5. Сборка виджета

```bash
cd widget
npm run build
cd ..
```

### 6. Запуск приложения

```bash
npm run dev
```

Приложение будет доступно на http://localhost:3000

## 📖 Использование

### Для владельцев сайтов

1. Войдите через GitHub на http://localhost:3000
2. Добавьте новый проект, выбрав репозиторий
3. Нажмите "Синхронизировать" для импорта коммитов
4. Отредактируйте черновики и опубликуйте их
5. Скопируйте код для встраивания и добавьте на свой сайт

### Встраивание виджета

Добавьте этот код перед закрывающим тегом `</body>`:

```html
<script src="http://localhost:3000/widget.js" data-id="YOUR_API_KEY"></script>
```

API ключ можно найти в настройках проекта.

## 🏗️ Структура проекта

```
commit-log/
├── app/                      # Next.js App Router
│   ├── api/                  # API Routes
│   │   ├── auth/            # NextAuth
│   │   ├── sync/            # Синхронизация с GitHub
│   │   ├── changelog/       # CRUD чейнджлогов
│   │   ├── projects/        # CRUD проектов
│   │   ├── repositories/    # Список репозиториев
│   │   └── v1/logs/         # Публичный API для виджета
│   ├── dashboard/           # Дашборд
│   └── page.tsx             # Лендинг
├── lib/                     # Утилиты
│   ├── prisma.ts           # Prisma клиент
│   └── github.ts           # GitHub API сервис
├── prisma/                  # Prisma схема
├── widget/                  # Встраиваемый виджет
│   ├── src/
│   │   └── index.ts        # Код виджета
│   ├── vite.config.ts      # Vite конфигурация
│   └── package.json
└── public/                  # Статические файлы
    └── widget.js           # Собранный виджет
```

## 🔑 API Endpoints

### Внутренние (требуют аутентификации)

- `POST /api/sync` - Синхронизация коммитов
- `GET /api/repositories` - Список репозиториев пользователя
- `POST /api/projects` - Создание проекта
- `GET /api/projects/:id` - Получение проекта
- `GET /api/projects/:id/changelogs` - Список чейнджлогов
- `GET /api/changelog/:id` - Получение чейнджлога
- `PUT /api/changelog/:id` - Обновление чейнджлога
- `DELETE /api/changelog/:id` - Удаление чейнджлога

### Публичные

- `GET /api/v1/logs?key=API_KEY` - Получение опубликованных чейнджлогов для виджета

## 🎨 Conventional Commits

Система распознаёт следующие типы коммитов:

- `feat:` → **Новое** (зелёный бейдж)
- `fix:` → **Исправлено** (синий бейдж)
- `improvement:` или `refactor:` → **Улучшено** (серый бейдж)

Пример:
```
feat: добавлена тёмная тема
fix: исправлена ошибка загрузки
improvement: оптимизирована производительность
```

## 💰 Монетизация

### Бесплатный тариф
- 1 проект
- До 10 записей в истории
- Брендинг "Powered by CommitLog"

### Pro тариф ($9/месяц)
- Безлимитные проекты
- Безлимитные записи
- Без брендинга
- Кастомный CSS (планируется)

## 🚀 Деплой на Vercel

1. Создайте проект на Vercel
2. Подключите GitHub репозиторий
3. Добавьте переменные окружения
4. Деплой!

Не забудьте обновить `NEXTAUTH_URL` и `NEXT_PUBLIC_APP_URL` на production URL.

## 📝 Лицензия

MIT

## 🤝 Поддержка

Если у вас возникли вопросы, создайте issue в GitHub репозитории.
